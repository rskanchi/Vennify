# To set up the environment for the app, you need to source("renv/activate.R")
# To be able to source renv/activate.R, you need to have renv installed.
# If you do not have renv installed, you can install it using and restore the environment using the following commands
# library(renv)
# renv::restore()

 
# Uncomment the line below to activate the renv environment
# source("renv/activate.R")
